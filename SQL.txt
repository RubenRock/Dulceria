//ALTER TABLE INVENTARIO_EMPAQUE ALTER COLUMN ID TYPE VARCHAR(50);
//cambiar manualmente el tipo de id de inventario_empaque a VARCHAR(50)


ALTER TABLE extras ADD MANEJAR_INVENTARIO SMALLINT;
ALTER TABLE extras ADD VERIFICAR_FECHA SMALLINT;
ALTER TABLE extras ADD VERSION VARCHAR(20);
ALTER TABLE extras ADD NOTAS VARCHAR(30);
ALTER TABLE extras ADD TIQUET VARCHAR(30);
ALTER TABLE extras ADD IMPRIMIR_EN_PANTALLA VARCHAR(2);
ALTER TABLE extras ADD NEGOCIO VARCHAR(35);
ALTER TABLE inventario_aux ADD CLAVE VARCHAR(50);

ALTER TABLE REMISIONES ADD CLAVE VARCHAR(50);
ALTER TABLE RECEPCIONES ADD CLAVE VARCHAR(50);
ALTER TABLE DEVOLUCIONES ADD CLAVE VARCHAR(50);
ALTER TABLE REMISIONES_RESP ADD CLAVE VARCHAR(50);
ALTER TABLE RECEPCIONES_RESP ADD CLAVE VARCHAR(50);

ALTER TABLE REMISIONES ADD CLAVE_EMPAQUE VARCHAR(50);
ALTER TABLE RECEPCIONES ADD CLAVE_EMPAQUE VARCHAR(50);
ALTER TABLE DEVOLUCIONES ADD CLAVE_EMPAQUE VARCHAR(50);
ALTER TABLE REMISIONES_RESP ADD CLAVE_EMPAQUE VARCHAR(50);
ALTER TABLE RECEPCIONES_RESP ADD CLAVE_EMPAQUE VARCHAR(50);

ALTER TABLE CLIENTES ALTER COLUMN DIRECCION TYPE VARCHAR(100);


CREATE TABLE CAJA_LISTA_REMISION(
    FOLIO int NOT NULL,
    CLIENTE varchar(35),
    TOTAL NUMERIC(15,2),
    FECHA DATE,
    VENDEDOR varchar(35),
    CONDICION varchar(35),	
    DOMICILIO varchar(30),
    IMPRESION varchar(20),
    DESCUENTO DECIMAL(15, 2),
    PRIMARY KEY (FOLIO)
);
CREATE TABLE CAJA_REMISIONES(
    FOLIO int,
    CANTIDAD int,	
    PRODUCTO2 VARCHAR(150),	
    TOTAL VARCHAR(20),    
    PRECIO varchar(20),
    TIPO varchar(20),
    DESCUENTO VARCHAR(10),
    PRODUCTO VARCHAR(70),
    CLAVE VARCHAR(50),
    IVA varchar(2)
);

ALTER TABLE INVENTARIO_AUX ALTER COLUMN PIEZAS TYPE DECIMAL(12, 2);
ALTER TABLE REPARTO ADD DESCRIPCION VARCHAR(20);
ALTER TABLE CAJA_LISTA_REMISION ADD DESCUENTO DECIMAL(15, 2);


//DUPLICAR TABLAS DE LISTA REMISION Y REMISIONES AGREGARLES "_REIMPRESION" A AMBAS

ALTER TABLE INVENTARIO ADD FACTURABLE VARCHAR(10);
ALTER TABLE EXTRAS ADD SEGUNDACAJA VARCHAR(2);

ALTER TABLE LISTA_REMISION ADD CAJERO VARCHAR(20);
ALTER TABLE LISTA_REMISION_RESP ADD CAJERO VARCHAR(20);
ALTER TABLE LISTA_REMISION_REIMPRESION ADD CAJERO VARCHAR(20);

ALTER TABLE LISTA_REMISION ADD METODO VARCHAR(20);
ALTER TABLE LISTA_REMISION_RESP ADD METODO VARCHAR(20);
ALTER TABLE LISTA_REMISION_REIMPRESION ADD METODO VARCHAR(20);

ALTER TABLE ABONOS ADD METODO VARCHAR(20);

ALTER TABLE LISTA_CORTES ADD SERVICIOS VARCHAR(30);
ALTER TABLE LISTA_CORTES ADD TERMINAL VARCHAR(30);
ALTER TABLE LISTA_CORTES ADD TRANSFERENCIA VARCHAR(30);
ALTER TABLE LISTA_CORTES ADD CHEQUE VARCHAR(30);

UPDATE LISTA_CORTES SET SERVICIOS ='0.00', TERMINAL ='0.00', TRANSFERENCIA ='0.00', CHEQUE ='0.00';

ALTER TABLE REPARTO ADD ADICIONAL VARCHAR(150);

alter table lista_cortes add retiros varchar(30);

UPDATE LISTA_CORTES SET retiros ='0.00';

CREATE TABLE RETIROS (
  FECHA DATE NOT NULL PRIMARY KEY,
  CAJERO VARCHAR(30) NOT NULL,
  CANTIDAD VARCHAR(20) NOT NULL);

ALTER TABLE LISTA_REMISION ADD FECHA_COBRO DATE;
ALTER TABLE LISTA_REMISION_RESP ADD FECHA_COBRO DATE;
ALTER TABLE LISTA_REMISION_REIMPRESION ADD FECHA_COBRO DATE;
update LISTA_REMISION set fecha_cobro = fecha;

-------***************---------------------------

ALTER TABLE INVENTARIO ADD EXISTENCIA INT;
update INVENTARIO set existencia = 1;

CREATE TABLE EXISTENCIAS(
  CLAVE VARCHAR(50) NOT NULL PRIMARY KEY,
  PRODUCTO VARCHAR(70),
  EXISTENCIA INT );

insert into EXISTENCIAS (producto, clave, EXISTENCIA) SELECT PRODUCTO, CLAVE,'0' FROM INVENTARIO
